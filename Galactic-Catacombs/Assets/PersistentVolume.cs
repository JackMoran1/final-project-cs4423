using UnityEngine;

public class PersistentVolume : MonoBehaviour
{
    private static PersistentVolume instance = null;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else if (instance != this)
        {
            Destroy(gameObject);
        }
    }
}
